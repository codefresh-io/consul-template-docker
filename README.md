# consul-template-docker

A minimal consul-template container which exposes a volume 
from which generated config files can be mounted by other containers.

Includes plugins: helper plugins for different environments
